/* eslint-disable import/no-anonymous-default-export */
export default {
    theme: {
        extend: {
            fontFamily: {
                brand: ['var(--font-brand)'],
            },
        },
    },
}
